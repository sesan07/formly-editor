import { FieldNamePipe } from './field-name.pipe';

describe('FieldNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldNamePipe();
    expect(pipe).toBeTruthy();
  });
});
