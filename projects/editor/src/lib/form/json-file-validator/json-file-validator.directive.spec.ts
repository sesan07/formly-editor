import { JsonFileValidatorDirective } from './json-file-validator.directive';

describe('JsonFileValidatorDirective', () => {
  it('should create an instance', () => {
    // const directive = new JsonFileValidatorDirective();
    // expect(directive).toBeTruthy();
  });
});
