export enum AppFieldType {
    CHECKBOX = 'checkbox',
    FORMLY_GROUP = 'formly-group',
    INPUT = 'input',
    NUMBER = 'number',
    INTEGER = 'integer',
    RADIO = 'radio',
    SELECT = 'select',
    TEXTAREA = 'textarea',
}

export enum AppWrapperType {
    FORM_FIELD = 'form-field',
}
